# NovelShell 2.0
A simple, clean, minimal shell for Hyprland + Arch Linux, that just works.

### Quick Start:
1. Clone the github repo
2. ```cd``` into the directory
3. Run the ```install.sh``` file
4. Run the command: ```qs``` in the terminal
5. If everything is running smoothly, you might want to add the ```qs``` command as autolaunch to your Hyprland config (if you want to keep using the shell)

### Features
Here are the features under the shell:
1. Workspaces Display
2. DateTime Display + Dropdown Calendar
3. Wifi Status + Wifi Configuration
4. Bluetooth Status + Bluetooth Configuration
5. Battery Status + Battery Configuration 
6. Volume Display + Media Player Configuration 
7. System Tray Dropdown
